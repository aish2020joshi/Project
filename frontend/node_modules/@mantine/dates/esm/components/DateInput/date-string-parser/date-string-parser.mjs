"use client";
import dayjs from "dayjs";
//#region packages/@mantine/dates/src/components/DateInput/date-string-parser/date-string-parser.ts
function dateStringParser(dateString) {
	if (dateString === null) return null;
	const date = new Date(dateString);
	if (Number.isNaN(date.getTime()) || !dateString) return null;
	return dayjs(date).format("YYYY-MM-DD");
}
//#endregion
export { dateStringParser };

//# sourceMappingURL=date-string-parser.mjs.map